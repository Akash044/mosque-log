rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Any signed-in user can read/write. For tighter control, keep an
    // admins/{uid} doc and require it to exist for writes.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
